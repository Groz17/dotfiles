#+property: header-args :tangle (concat (getenv "XDG_CONFIG_HOME") "/opencode/opencode.json")

#+begin_src jsonc :prologue "{" :epilogue "}"
"$schema": "https://opencode.ai/config.json",
"theme": "aura",
"mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
#+end_src
